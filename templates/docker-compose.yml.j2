# Top-level compose file for shared services.
# Individual application services are passed in by Ansible.
services:
  caddy:
    build: /opt/docker/caddy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - /opt/docker/caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config: